<% if form.errors.any? %>
  <div id="errors">
    <h2><%= pluralize(form.errors.count, t(:error)) %> <%= t(:prohibited_record_save) %>:</h2>
    <ul>
    <% form.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>
